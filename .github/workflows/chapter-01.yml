name: Chapter 01 CI

on:
  push:
    paths:
      - "chapter-01-6dof-equations/**"
      - ".github/workflows/chapter-01.yml"
  pull_request:
    paths:
      - "chapter-01-6dof-equations/**"
      - ".github/workflows/chapter-01.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: chapter-01-6dof-equations

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run unit tests
        run: pytest -q

      - name: Run simulation smoke test
        run: python main.py
